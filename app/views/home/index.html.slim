= title "#{t'site.name'} － 专注大盘竞猜"
= notice_info
= render 'vote_form' if logged? and voted?(get_target_date_all_ok) and @user.current_vote.content.blank?
= render 'vote_result'
= render 'good_players'